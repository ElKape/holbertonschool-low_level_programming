#include "main.h"

/**
 * int _abs(int) - Function that computes the absolute value of an integer.
 * @n: is a number
 * Return: Always 0.
 */

int _abs(int n)
{
	if (n >= 0)
		return (n);
	else
		return (-n);
}
