#include "main.h"

/**
 * print_numbers - Function that that prints the numbers, from 0 to 9, followed by a new line.
 *
 * Return: null.
 */

void print_numbers(void)
{
	int n;
	if (n <= 0 && >= 9)
	{
		_putchar('n')
	}
	else
	_putchar('\n')
	return (0);
}
